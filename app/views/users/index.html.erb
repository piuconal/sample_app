<% provide:title, t("flash.users.index.title")%>
  <h1><%= t("flash.users.index.heading") %></h1>

  <%== pagy_bootstrap_nav @pagy %>
  <ul class="users">
    <% @users.each do |user| %>
      <%= render @users %>
    <% end %>
  </ul>
<%== pagy_bootstrap_nav @pagy %>
