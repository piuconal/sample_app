<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("flash.users.profile_link"), current_user %></span>
<span>
  <%= pluralize(current_user.microposts.count, t("micropost.other", count: current_user.microposts.count)) %>
</span>
