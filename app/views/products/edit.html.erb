<h1><%= t('products.edit.title') %></h1>

<%= render "form", product: @product %>

<br>

<div>
  <%= link_to t('products.edit.show_product'), @product %> |
  <%= link_to t('products.edit.back_to_products'), products_path %>
</div>
